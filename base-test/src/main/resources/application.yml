server:
  port: 8081
spring:
  thymeleaf:
    prefix: classpath:/templates/

#数据库连接配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dblog
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  swagger：
    base-package：com.battcn  # 扫描的包路径,默认扫描所有
    enabled：true       # 默认为 true
